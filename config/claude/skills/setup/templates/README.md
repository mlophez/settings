# Project name

<!-- Replace the title with the project name. -->

## Description

<!-- What the project is and what it does, 2-4 sentences. Reuse the purpose
from the general context block. -->

## Requirements

<!-- Prerequisites to work on the project: runtimes, SDKs, tools, accesses.
Bullet list with versions. -->

## Setup

<!-- Steps to get the project running locally for the first time:
clone, install dependencies, local configuration. Numbered list with the
exact commands; use placeholders like <repo-url> for unknown values. -->

## Usage

<!-- How to run/use the project: the run command and, if applicable,
basic usage examples (endpoints, CLI invocations...). The run command
goes here, not in Development. -->

## Development

<!-- Exact commands for the daily cycle: build, test, lint and format. Reuse the
answers from the commands and code conventions blocks. If the agent hooks were
installed, add that `scripts/agent/validate.sh` runs the full validation (lint,
build, tests) and that `hooks.sh` at the project root is the only place to edit
when tooling changes. -->

## Documentation

- [Agent and project reference](AGENTS.md) — architecture, code style, testing, security and design.
