# vim: filetype=mailcap

#audio/*; /usr/bin/xdg-open %s ; copiousoutput
#image/*; /usr/bin/xdg-open %s ; copiousoutput

# text/html; chromium %s; copiousoutput ; nametemplate=%s.html
text/html; w3m -I %{charset} -T text/html -cols 100 ; copiousoutput; nametemplate=%s.html
# text/html; qutebrowser %s; copiousoutput ; nametemplate=%s.html
text/html; cat ; copiousoutput
text/plain; cat ; copiousoutput
text/calendar; mutt-ics ; copiousoutput
application/ics; mutt-ics ; copiousoutput
audio/*; /usr/bin/xdg-open %s ; copiousoutput
image/*; gwenview %s ; copiousoutput
application/*; /usr/bin/xdg-open %s ; copiousoutput
#application/msword; /usr/bin/xdg-open %s ; copiousoutput
#application/pdf; zathura %s ; copiousoutput
#application/postscript ; zathura %s ; copiousoutput
#text/html; luakit '%s' &; test=test -n "$DISPLAY"; needsterminal;
#text/html; lynx -dump -display_charset UTF-8 -width 150 %s ; copiousoutput; nametemplate=%s.html
# text/html; qutebrowser %s && sleep 5 ; test=test -n "$DISPLAY";
#nametemplate=%s.html; needsterminal
# text/html; w3m -I %{charset} -T text/html ; copiousoutput; nametemplate=%s.html
